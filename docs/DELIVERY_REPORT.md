# VideoManager 0.7.2 交付与验收记录

## v0.7.2 更新（2026-09-21）

- 修复 mpv Windows 嵌入模式下默认禁用子窗口导致的鼠标输入丢失：承载进程仅启用自身承载窗口内的 mpv 子窗口，并在视频输出重建后恢复输入。
- 全屏控制器采用 mpv OSD 原生绘制，参照 Chromium 视频控制布局：渐变背景、全宽进度条、播放/暂停、静音、音量滑块、播放时间、倍速、封面及退出全屏。点击与拖动由 mpv 自身处理，移动鼠标再次唤起、静止 2 秒隐藏，拖动期间保持可见，显隐不改变视频尺寸。
- 将选中数量和收藏、标签、移动、回收站、取消选择图标移至筛选左侧的独立分区；未选中时隐藏，不再插入额外横条。窄窗口收紧浏览范围与媒体类型入口，工具栏保持单行固定高度。
- 版本更新为 0.7.2。按要求未运行测试；仅编译原生承载组件、执行生产编译及安装包构建，未进行运行验证。
- 安装包：release/VideoManager-0.7.2-Windows-x64-Setup.exe。


## v0.7.1 更新（2026-09-21）

- mpv 全屏启用原生 OSC 时等待脚本确认，并在视频承载区域完成尺寸同步后主动显示控件；扩大鼠标唤起区域，控件浮于视频上，显隐不改变画面尺寸。音量滚轮操作显示原生 OSD 数值。
- 压缩筛选面板纵向间距、输入框与标签高度，在较小窗口中优先保留操作按钮；视频时长输入使用分钟，底层换算为秒，保留旧筛选条件语义。
- 媒体右键菜单以鼠标位置为锚点，锚点放在独立 Portal 中，避开虚拟列表变换及横向溢出；增加窗口边缘避让和高度约束，空间不足时菜单内部滚动。
- 发布包按运行依赖清单收录原生组件，移除未使用的 avfilter/avdevice、mpv 手册和安装辅助脚本、SQLite/Sharp 构建源码、类型声明与 source map；Electron 语言资源保留简体中文、繁体中文和英语，保留运行库与许可文件。
- 按用户要求不运行测试，只执行构建所需类型检查、生产编译和打包。未启动软件进行运行验证。
- 安装包：release/VideoManager-0.7.1-Windows-x64-Setup.exe。


## v0.7.0 更新（2026-09-20）

- mpv 全屏完全移除网页底栏，使用原生 OSC 浮动控制器提供进度、音量、音轨、字幕及封面操作；使用精确定位并禁用视频边距调整，控件显隐不再改变视频区域尺寸。退出全屏仍回到同一个播放弹窗。
- 标题栏窗口按钮背景与界面画布保持一致，覆盖应用启动、切换媒体库、手动切换主题及系统深浅主题变化。
- 筛选面板按标签、文件大小、修改日期、视频时长分区，标签横向显示，提供标签查找、范围校验、重置与统一应用；日期按本地自然日计算。
- 详细列表增加独立时长列，将「大小 / 内容」改为「大小」，文件夹大小显示「—」；列头支持拖动排序、拖动调整宽度、双击恢复默认宽度及键盘调整，配置本地保存。
- 列表封面调整为 64 × 40px，在 48px 行高中垂直居中；列表与网格共用勾选标记，移除影响勾选图标的旧样式。
- 按用户要求不执行测试；仅执行编译与安装包构建，未进行运行验证。
- 安装包：release/VideoManager-0.7.0-Windows-x64-Setup.exe。



## v0.6.6 更新（2026-09-20）

- 移除 mpv 画面两侧固定 60px 留白，按解码输出的显示比例布局；全屏画面使用可用空间并保留视频原始比例。
- 进度条改为按指针坐标计算时间、捕获拖动并在松开时提交精确绝对定位；等待 mpv 完成跳转后读取实际时间，再恢复进度同步，避免旧进度覆盖待定位时间。
- 全屏默认隐藏功能栏；通过原生画面鼠标通知和界面指针事件，在鼠标移到底部时显示进度及封面等操作，静止 2 秒后隐藏。控制栏独占底部布局空间，显示时视频区域缩小，不覆盖视频内容。
- 资源状态点独立于主题颜色：在线绿色，离线/路径异常红色，扫描/检查黄色，索引未完成橙色，未知状态灰色。
- 版本更新为 0.6.6。按用户要求未执行测试，仅编译原生组件、生产构建及打包；不将历史版本测试结果作为本版本验证。
- 安装包：release/VideoManager-0.6.6-Windows-x64-Setup.exe。

## v0.6.5 更新（2026-09-20）

- 修复推荐候选帧与逐帧解码器位置不一致：逐帧操作先对齐当前选中时间，再按真实解码帧前后移动；切换候选时恢复对应视频来源，避免旧来源或异步结果覆盖选中画面。视频推荐只使用实际解码时间，避免将 PTS 数值当作秒数。
- 新增主题强调色调色板，包含六种预设、自定义颜色及恢复默认，保存至当前媒体库；深浅主题自动调整强调色对比度。
- 移除浏览密度设置及相关布局分支，列表使用统一间距，保留卡片尺寸设置。
- mpv 改为原生子窗口嵌入同一个播放弹窗，通过 vm-player-host 承载画面；控制栏提供暂停、时间轴、倍速、音量、字幕/音轨切换、全屏和「设为封面」，不再弹出独立 mpv 播放窗口。
- 版本号更新至 0.6.5。按用户要求不执行测试；仅编译原生组件、完成生产构建及安装包打包。以下旧版本的测试记录不代表本版本经过运行验证。
- 交付路径：release/VideoManager-0.6.5-Windows-x64-Setup.exe。

## v0.6.0 播放器插件与目录折叠（2026-09-19）

- 新增三选一的视频播放插件：Chromium、mpv、系统默认应用。在「设置与插件 → 功能插件 → 视频播放方式」切换，选择保存在当前媒体库中；默认保持 Chromium。
- mpv 随安装包分发，以独立原生窗口播放，通过本机命名管道提供暂停、定位、倍速、音量、全屏和进度同步。关闭播放器、切换视频或退出应用时保存进度并回收本应用启动的进程。系统默认应用遵循 Windows 文件关联，进度由该应用管理。
- 修复目录祖先自动展开覆盖手动折叠的问题；选中目录或其祖先现在可以折叠，右侧浏览位置保持不变，导航到其他位置时仍能自动展开对应路径。
- 必要验证：TypeScript 类型检查与生产构建；2 项插件测试；隔离媒体库验证目录折叠、mpv 实际 TS 播放/暂停/定位/倍速/续播、旧会话隔离、系统文件关联分发及 Chromium MP4 播放。系统默认应用调用使用测试替身，未启动用户关联的应用。未运行全量回归或性能压力测试。
- 测试范围说明：TS 样本在旧有封面提取链路可能报告候选解码失败；本次播放器回归只要求完成文件索引并验证实际播放，未将其计为封面兼容性验收。
- 打包应用冒烟通过：v0.6.0 正常启动，实际调用 resources/native/mpv 中的运行时播放 TS。记录见 test-results/v060-package.json；未重复执行完整功能回归或安装/卸载流程。
- 安装包：release/VideoManager-0.6.0-Windows-x64-Setup.exe（210,795,945 字节），SHA-256 为 48670e717b7f4e4c96808194a3682e6341513ef65fcdbf97bd305036cef2f1cd。功能记录见 test-results/v060-regression.json；运行时来源和许可见 native/mpv-dependency.json、docs/THIRD-PARTY-NOTICES.md。

## v0.5.0 修复与必要验证（2026-09-19）

- 封面模糊根因：480px PNG 被再次缩至 240px 并以 WebP quality=80 编码；卡片和高 DPI 显示又将其放大。现按封面质量生成无损 WebP，默认 768px；设置与编辑器均按原图、1280、768、480、240 降序排列。已有手动快照和手动质量保留，旧离线缩略图仍可回退使用。
- 浏览路径持久化不再发送全局 changed；目录树刷新保留原数据，首屏分页提前读取，封面替换先解码再显示，避免刷新时闪空。
- 修改封面后直接复用保存的快照更新自动祖先封面，不清空封面再遍历/解码候选；手动祖先封面不变。自动候选优先手动快照、其次图片，避免低分辨率视频预览压过目录内高清图片。
- 目录索引改为动态队列、分页写入、父级提交后立即调度子级，取消逐层屏障和整树扫描后串行入库；未变条目跳过全文索引及闭包重建。SQLite 仍通过单写进程保证一致性。
- 图像后台线程池支持最多 16 个作业；封面队列调高并发时立即填充空闲槽；目录封面按深度复用已生成的子级封面，去掉最多 2 路的限制；自动视频候选由 5 帧降为 3 帧，手动推荐保留 12 帧。
- 必要验证：TypeScript 类型检查；数据库及调度回归 13 项；隔离 Electron 样本验证目录/媒体计数、实际预览像素、无损编码、祖先封面刷新、目录节点保持挂载、默认尺寸保存和降序选项。未执行全量性能、跨盘移动或破坏性文件操作测试。
- 最终打包应用隔离样本（32 张图片、1 个视频、16 个目录）：4 路索引及封面约 1.22 秒，父级新封面加载约 54ms。这是功能验证样本，不代表各类磁盘和媒体库的通用加速倍数。测试环境使用软件渲染以避开受限运行环境的 GPU 子进程启动问题。
- 安装包：release/VideoManager-0.5.0-Windows-x64-Setup.exe；详细功能结果见 test-results/v050-regression.json。


## 历史验收基线（以下未在 v0.5.0 全量重跑）

日期：2026-09-18。需求基线：本目录 `PRD.md`、`TRD.md`。交付为可安装、可直接运行的 Windows x64 开发验收版本；源码、原生组件、构建脚本、测试与安装包一并提供。

## 功能落地

| 需求 | 实现与验证 |
| --- | --- |
| FR-01 资源与索引 | 根目录添加/合并、分页扫描、文件监听与定期补扫、暂停/取消、离线保留、稳定身份；实际十万文件三轮索引 |
| FR-02 分区展示 | 直属文件夹/媒体分区、后代媒体总览、来源路径、非媒体计数、虚拟网格/列表 |
| FR-03 浏览与查看 | 多选/快照全选、导航历史、同级路径、图片缩放旋转、视频播放/倍速/续播/全屏、重启恢复浏览上下文 |
| FR-04 查询与组织 | 参数化 SQL、FTS5 与短中文回退、维度组合筛选、自然排序、标签/收藏、不可变结果快照 |
| FR-05 封面 | 本地非 AI 推荐、实际 PTS 选帧、VFR/B 帧步进、16:10 裁剪、内容寻址原始快照、手动锁定、来源不可用仍可再次裁剪 |
| FR-06 文件操作 | 影响预览、源身份/版本复核、禁止覆盖、整目录移动、跨卷 SHA-256 双重校验、源句柄保留、系统回收站、操作日志与启动恢复 |
| FR-07 插件 | Cordis 适配层、内置导航/布局独立切换、JSON Schema 配置校验、依赖检查、启动回滚、基础导航/列表回退、可信本地开发插件入口 |
| FR-08 管理迁移 | SQLite 在线备份快照、校验管理包、封面对象流式读写、独立库导入、重复导入选择、根目录身份/指纹重绑、缓存与手动对象分离 |

安全边界：渲染进程开启沙箱与 contextIsolation，关闭 Node 集成；固定 preload 方法、来源验证、Zod 输入校验；媒体使用不透明授权令牌和 Range 流；FFmpeg 使用本地文件句柄自定义 AVIO 并拒绝嵌套资源打开。运行中不调用 AI 服务，不上传媒体，不执行在线遥测。

## 已执行验证

1. TypeScript 类型检查与生产构建通过。
2. 22 项领域、SQLite、Cordis、Win32 文件安全和 FFmpeg 原生测试通过。
3. 桌面端到端回归：真实扫描、分区/去重、插件切换保留选择、中文搜索与组织、Range 206、MP4 H.264/AAC、WebM VP9/Opus、真实帧步进与封面持久化、实际目录重命名、文件移动、系统回收站、管理包导入导出/重绑、明暗主题、1280×720、重启持久化。已在打包后的 EXE 上运行。
4. 补充回归：右键菜单、来源移出后的快照再次裁剪、封面编辑器播放暂停选帧、WebM VP8/Vorbis 播放和选帧、重启恢复搜索及选择、后台元数据与缓存配置。
5. F: 与 C: 之间实际跨卷测试：整目录含非媒体文件移动并保留目录 ID/收藏；模拟只读源删除失败后两个副本摘要相同；64 MB 文件取消后保留源。
6. 包管理器依赖审计：修正 Vitest 测试依赖后，npm audit 报告 0 个已知漏洞。这不替代原生组件或完整应用安全审计。
7. 最终 NSIS 安装包在项目内独立目录完成静默安装、启动到欢迎页、受限桥接验证及卸载；安装和卸载退出码均为 0。测试副本已卸载，交付文件保留。原始记录为 `test-results/install-smoke.json`。

原始记录在 `test-results/`：`e2e-report.json`、`extra-e2e.json`、`cross-volume.json`、`performance.json`、`desktop-performance.json`、`media-performance.json`。预览生成完成后的最终截图为 `release-dark.png`、`release-light.png`；另有 `cover-editor-dark.png`、`browse-1280x720.png`。具体运行时版本见 `docs/versions.json`。

## 实测性能及口径

本机为 Intel Core i9-13900HX、16 GB 内存、Windows 11 x64；并非 PRD 指定的 6 核 12 线程标准机器。以下是实际结果，不替代完整 P0 性能验收。

| 指标 | 本机记录 | 测量范围 |
| --- | --- | --- |
| 十万文件基础索引 | 169.381 / 169.258 / 174.127 秒 | 3 个独立库；100,000 个零字节发现样本，10,100 个目录；不包含完整解码 |
| 新目录首批 50 项 | 355 / 333 / 347 ms | 从添加目录到后台报告已处理至少 50 项，含 IPC 轮询误差 |
| 启动到受限桥接就绪 | 1,145 / 1,150 / 1,184 ms | 不等同于上次大目录首屏全部可交互的 P95 |
| 十万条短中文查询 | IPC P95 498 / 515 / 493 ms | 每轮 30 次，查询物化和首批 100 条返回；不含完整 UI 提交时间 |
| SQLite 纯查询 | 中文全量 P95 213.01 ms；后代按时间 P95 252.93 ms | 每种 30 次热查询；查询服务内计时 |
| 1080p H.264 实际选帧 | P50 191.8 ms；P95 318.4 ms；最大 324.5 ms | 30 次，原生 seek/解码/PNG 与 Sharp 归一化；不含 UI 绘制 |

规模文件用于基础索引，不伪装成 100,000 个真实可解码媒体。媒体回归使用生成的 JPEG/PNG/WebP/GIF/BMP、H.264/AAC、VP8/Vorbis、VP9/Opus、VFR/B 帧及损坏样本；另有 1080p 样本。

复现命令：`node scripts/desktop-benchmark.mjs`；`npx vitest run tests/performance.test.ts`；`npx vitest run tests/media-benchmark.test.ts`。跨卷测试：设置 `VM_CROSS_VOLUME_ROOT` 为另一卷的专用测试目录，再运行 `node scripts/cross-volume.mjs`。脚本会创建隔离文件夹。

## 发布前仍需完成的验收与限制

- 尚未完成 PRD 要求的至少 200 个真实媒体样本、标准 6 核机器冷/热 UI P95、滚动长任务、5 分钟全进程 CPU/内存、长时间压力、150%/200% DPI、移动盘断连、全阶段崩溃注入、升级/降级矩阵。不能将本机冒烟与自动化结果称为全部 AC-01～AC-16 正式验收通过。
- 当前为首个数据结构版本；拒绝更高版本数据库/管理包。尚无可验证的历史版本升级链。后续结构升级必须补上升级前备份、失败恢复与旧版本样本测试。
- FFmpeg 动态库来自固定摘要的 LGPL shared 构建。应用的输入通道禁止网络与嵌套输入，但上游构建本身仍包含网络协议能力，尚不是 TRD 所要求的专门关闭网络组件的自构建发行物。公开发布前需要归档对应源码/构建脚本与全部第三方许可，并完成该原生发行构建和签名。
- 内置插件功能已经连通。开发模式当前提供可信宿主模块和槽位注册，尚未交付任意第三方自定义 React UI 模块的完整挂载协议、插件卸载管理或插件市场；开发插件不能被视为不可信代码沙箱。
- 管理包封面对象采用流式处理；索引 JSONL 解析仍在受限内存预算内完成（记录合计 256 MB、单成员 256 MB、解压总量 2 GB）。超限明确拒绝，超大管理包的全程增量记录导入仍需后续扩展。
- 不确定的崩溃恢复项会保留文件与日志并在任务中心提示人工核对；不是所有故障阶段都有一键重试入口。跨卷链接、备用数据流、加密文件等不安全情况会拒绝或保留源，不宣称完整支持 NTFS 所有特殊属性。
- 安装包未签名，未使用代码签名证书。首版提供真实可用功能及可复核测试，不应在未补齐上述条件时标记为正式 P0 发布认证。

依赖精确版本记录于 `package.json`、两个锁文件及 `native/dependencies.json`；发行校验值生成到 `release/artifacts.json` 和 `release/SHA256SUMS.txt`。
